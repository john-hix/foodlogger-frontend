export default function Client() {
    // @TODO: wrap axios methods like
    // Client.users.get(), where get is a thin wrapper over axios which
    // eliminates the need to specify the endpoint
    // Also, Client.user.get(id)
    // Client.user.create({}: reqBody, {}: axiosConfig), as with axios.post, just w/o the endpoint 

    // Don't go crazy. Use OpenAPI spec to do code gen if you want to go crazy.
}